<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network permissions required by the REST client -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!--
            ATAK plugin descriptor activity.
            The metadata entries below are read by the ATAK host application to
            discover and load this plugin at runtime.
        -->
        <activity
            android:name=".ScytheLifecycle"
            android:exported="true">
            <intent-filter>
                <action android:name="com.atakmap.app.LIFECYCLE" />
            </intent-filter>
            <meta-data
                android:name="com.atakmap.app.pluginVersion"
                android:value="1" />
            <meta-data
                android:name="com.atakmap.app.DESCRIPTION"
                android:value="@string/plugin_description" />
        </activity>

    </application>

</manifest>
